---
name: Agent OS Memory and Protocol
overview: "Build four interconnected systems in the REMI agent kernel: (1) episodic memory with post-run extraction, (2) semantic memory that compounds across sessions, (3) a formalized agent-to-agent protocol with Agent Cards, typed task lifecycle, and context threading, and (4) context compression beyond the current trim-and-flush approach. Together these turn REMI from a stateless agent runner into a persistent, self-improving, multi-agent operating system."
todos: []
isProject: false
---

# Agent OS: Memory, Protocol, and Context Compression

## Architecture Overview

Four systems, layered on existing infrastructure:

```mermaid
graph TB
  subgraph kernel ["agent/ (kernel)"]
    direction TB
    Memory["
```



