# LLM Providers (at least one required for AI features)
OPENAI_API_KEY=
ANTHROPIC_API_KEY=
GOOGLE_API_KEY=

# Database — local dev (docker compose up -d, then REMI_CONFIG_ENV=dev remi serve)
DATABASE_URL=postgresql://remi:remi@localhost:5433/remi
# Database — production
# APPOS_DATABASE_URL=postgres://user:pass@host:5432/remi

# API (used by sandbox client to call back into REMI)
# REMI_API_URL=http://127.0.0.1:8000
