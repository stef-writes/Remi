environment: local

state_store:
  backend: in_memory

logging:
  level: DEBUG
  format: console

execution:
  max_retries: 1
  retry_delay_seconds: 0
  concurrency_limit: 4

api:
  host: 127.0.0.1
  port: 8000
