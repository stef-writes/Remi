environment: development

state_store:
  backend: in_memory

logging:
  level: INFO
  format: structured

execution:
  max_retries: 3
  retry_delay_seconds: 1
  concurrency_limit: 10
  idempotency: true

api:
  host: 0.0.0.0
  port: 8000
  cors_origins:
    - "http://localhost:3000"
