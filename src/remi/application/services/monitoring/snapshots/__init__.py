"""Snapshots category — time-series rollups and trend infrastructure.

``SnapshotService`` captures periodic manager/property snapshots.
``RollupStore`` defines the persistence port.  ``metrics`` provides
shared occupancy/LTL computation helpers used by both snapshots and
query services.
"""
