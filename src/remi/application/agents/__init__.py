"""Agents — AI agent YAML manifests.

Each subdirectory contains an ``app.yaml`` defining an agent's persona,
tools, delegation graph, and runtime configuration.

    director/app.yaml         Director agent manifest
"""
