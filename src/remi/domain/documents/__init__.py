"""Document domain — upload, parse, and store structured data."""

from remi.domain.documents.models import Document as Document
from remi.domain.documents.models import DocumentStore as DocumentStore
